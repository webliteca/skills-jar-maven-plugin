name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: [ '11', '17' ]

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'
          cache: 'maven'

      - name: Build and test
        run: mvn -B verify

      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports-jdk${{ matrix.java }}
          path: |
            target/surefire-reports/
            target/invoker-reports/
            target/it/*/build.log

# TODO: Release workflow
# A release.yml that triggers on version tags (v*) should:
# 1. Build with mvn -B verify
# 2. Configure GPG signing (via secrets)
# 3. Deploy to Maven Central via OSSRH (mvn -B deploy -P release)
# This requires:
# - GPG_PRIVATE_KEY and GPG_PASSPHRASE secrets
# - OSSRH_USERNAME and OSSRH_TOKEN secrets
# - A <release> profile in pom.xml with maven-gpg-plugin and nexus-staging-maven-plugin
