# Configuration Guide

This is a configuration reference document for testing.

## Settings

- `enabled` — whether the feature is enabled (default: true)
- `timeout` — timeout in milliseconds (default: 5000)
